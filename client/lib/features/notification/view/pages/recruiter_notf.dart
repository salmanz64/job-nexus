import 'package:flutter/material.dart';

class RecruiterNotf extends StatelessWidget {
  const RecruiterNotf({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
